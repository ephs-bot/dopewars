/**
 * Dope Wars - Entry Point
 * Initializes the game and shows the setup screen.
 */

import { renderSetupScreen } from './ui.js';

document.addEventListener('DOMContentLoaded', () => {
    renderSetupScreen();
});
